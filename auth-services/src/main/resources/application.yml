spring:
  application:
    name: login-service

  datasource:
    url: jdbc:postgresql://localhost:5432/SLK
    username: postgres
    password: slk@SOFT123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  web:
    cors:
      mappings:
        "/**":
          allowed-origins:
            - "http://localhost:4200"
          allowed-methods:
            - GET
            - POST
            - PUT
            - DELETE
            - OPTIONS
          allowed-headers:
            - "*"
          allow-credentials: true

server:
  port: 8082